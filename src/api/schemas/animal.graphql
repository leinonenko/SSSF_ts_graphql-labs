scalar DateTime

type Animal {
  id: ID!
  animal_name: String!
  species: Species!
}

type Query {
  animals: [Animal]
}

type Mutation {
  addAnimal(animal_name: String!, species: ID!): Animal

  modifyAnimal(id: ID!, animal_name: String, species: ID): Animal

  deleteAnimal(id: ID!): Animal
}
